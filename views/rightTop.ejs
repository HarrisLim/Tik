<script>
	function resend() {
		$('#submit_resend').submit();
	}
	function permission() {
		$('#submit_permission').submit();
	}
	
	function requestManager() {
		alert('You do not have an authority.\nPlz, Repuest to manager.');
	};

</script>	
<a href="/process/main"><h3>Talk<br>T ik</h3></a>
<hr>
<div style="float:right"> 
	<% if(nickname === "harrislim") {%> <%# permission code %>
		<a href="/process/authorize">Authorizing</a> | 
	<% } %>
	<% if(permission === null) { %> 
		<div class="ui form" style="text-align:right">
		<h3 style="color:red;margin:0px">you've not verified email yet.</h3>
		<h3 style="color:blue;margin:0px"><%=permissionMessage%></h3>
			<form method="post" action="/process/permission" id="submit_permission">
			</form>
			<form method="post" action="/process/resend" id="submit_resend">
			</form>
			<a href="javascript:permission();" style="color:red" id="permission">Permission</a> | 
			<a href="javascript:resend();" style="color:red" id="resend">Resend</a><br>
			<a href="/process/myinfo">My info</a> | 
			<a href="/process/signout">Sign out</a>
		</div>
	<% } else {%>
	<% if(countMypost < curPermissionpost) {%>
			<a href="/process/addpost">Add post</a> | 
		<% } else {%>
		<a href="" onClick='requestManager()'>Add post</a> | 
		<%} %>
		<a href="/process/mypost">My post</a> | 
		<a href="/process/myinfo">My info</a> | 
		<a href="/process/signout">Sign out</a>
	<% } %>
	<div style="text-align:right">
		<div><p><i class="address card icon"></i> <%= email%></div>
		<div><p><img src="../../flags-mini/<%=signTld%>.png"> <%= nickname%></div><br>
	</div>
</div>